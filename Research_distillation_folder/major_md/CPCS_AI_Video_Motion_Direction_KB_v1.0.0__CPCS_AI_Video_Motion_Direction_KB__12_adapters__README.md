# Model Adapters

These are **capability snapshots**, not SDK clients. A deployment must verify the exact first-party API or UI surface, request schema, account/tier/region, and official documentation before use. `request_template` is intentionally conceptual where official field names were not verified.

Adapter status expires after `ttl_days`. Legacy adapters remain readable for reproducibility but are blocked for new compilation by policy.
